#running shell script with github workflow
name: Check for push
on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: script
        run: 
         git clone --mirror https://vijeth_s01:tn+*VjZWM23@bitbucket.org/vijeth_s01/repoa.git 
        
      - name: printing the list
        run: |
          echo Here is the list
          ls -a
        
        
        shell: bash
